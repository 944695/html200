const firstName = "John"
const lastName = "Smith"
const driverAge = 40
const drivingAge = 16

if(driverAge >= drivingAge)
{
  console.log(`${firstName} ${lastName} can drive and has driven for ${driverAge - drivingAge} years.`)
}
else
{
  for(let i = 5; i >= 0; i--)
    console.log(i)
}